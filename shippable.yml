language: clojure

lein:
  - lein2

jdk:
  - openjdk8

git:
  submodules: false

build:
  pre_ci_boot:
    image_name: drydock/u14cloall
    image_tag: v5.8.4
    pull: true

  ci:
    - java -version
    - ./pre-commit.sh
