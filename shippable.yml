language: clojure

lein:
  - lein2

git:
  submodules: false

build:
  pre_ci_boot:
    image_name: drydock/u14cloall
    image_tag: latest
    pull: true

  ci:
    - java -version
    - ./pre-commit.sh
