language: clojure

lein:
  - lein2

jdk:
  - openjdk8

git:
  submodules: false

build:
  pre_ci_boot:
    image_name: drydock/u14cloall
    image_tag: v5.8.2

  ci:
    - java -version
    - ./pre-commit.sh
    - mv report.xml shippable/testresults
